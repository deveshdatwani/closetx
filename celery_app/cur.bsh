#!/bin/bash

# Run curl to localhost:5000 indefinitely
while true
do
  curl http://localhost:5000
  # Optional: Add a sleep to prevent too many rapid requests
  #sleep 1  # Sleep for 1 second (adjust as needed)
done

